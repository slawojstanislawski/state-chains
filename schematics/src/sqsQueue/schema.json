{
  "$schema": "http://json-schema.org/schema",
  "$id": "sqsQueue",
  "title": "SQS Queue schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the queue",
      "x-prompt": "What is the name of the queue (no spaces)?"
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
