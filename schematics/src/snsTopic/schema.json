{
  "$schema": "http://json-schema.org/schema",
  "$id": "snsTopic",
  "title": "SNS Topic schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the topic",
      "x-prompt": "What is the name of the topic (no spaces)?"
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
